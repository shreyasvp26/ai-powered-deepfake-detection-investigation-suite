# GPU-server requirements — L4 / CUDA 12.x environment.
# Installs torch with CUDA support + training-only deps.
# Does NOT include V2 web stack (FastAPI, Redis, etc.) — unnecessary on GPU box.
#
# Usage: pip install -r requirements-gpu.txt
#
# --- CUDA-enabled PyTorch (pinned per §4.3) ---
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.1.2+cu121
torchvision==0.16.2+cu121
torchaudio==2.1.2+cu121

# --- Core ML (matching requirements.txt pins) ---
timm==0.9.12
facenet-pytorch==2.5.2
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
wandb>=0.16.0
tensorboard>=2.14.0
PyYAML>=6.0.1
tqdm>=4.66.0
pytorch-grad-cam>=1.4.8
xgboost>=2.0.0
fpdf2>=2.7.0
Pillow>=10.0.0
matplotlib>=3.8.0
seaborn>=0.13.0

# --- Linux GPU only ---
opencv-python==4.9.0.80
insightface>=0.7
onnxruntime-gpu>=1.16.0

# --- Dev/testing ---
pytest>=7.4.0

# --- Streamlit (for Flask inference API, V1 surface) ---
flask>=3.0.0
requests>=2.31.0
