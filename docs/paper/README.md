## IEEE conference paper (Overleaf-ready)

This folder contains an **IEEE conference-style** LaTeX manuscript focused on **deepfake-type attribution** with **DSAN v3.1** as the central method.

### Files
- `main.tex`: main LaTeX entrypoint (IEEEtran conference).
- `references.bib`: BibTeX references.
- `figures/`: figure placeholders (add exported PDFs/PNGs here).

### Overleaf usage
1. Create a new Overleaf project.
2. Upload the entire `docs/paper/` folder (or upload `main.tex`, `references.bib`, and `figures/`).
3. Set `main.tex` as the main file.

### Notes
- IEEE template constraint is respected: **no math/symbols in the title or abstract**.
- Figures referenced by filename are placeholders; replace with your exported diagrams.

